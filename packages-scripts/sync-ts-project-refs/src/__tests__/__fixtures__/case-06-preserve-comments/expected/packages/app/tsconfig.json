{
  // App configuration
  "compilerOptions": {
    // Enable all strict checks
    "strict": true,
    "target": "ES2020"
  },
  // Project references to dependencies
  "references": [
    {
      "path": "../utils/tsconfig.json"
    }
  ]
}
